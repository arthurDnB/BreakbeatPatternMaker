import test from 'node:test';import assert from 'node:assert/strict';
import {newBank,arrange,validateBank} from '../dist/core/bank.js';
import {generate} from '../dist/core/generate.js';import {defaults} from '../dist/core/profiles.js';
import {renderPerformance,renderSequence} from '../dist/audio/performance.js';
import {makeProject,readProject} from '../dist/audio/project.js';import {defaultKitState} from '../dist/audio/drum-kit.js';
test('bank order, repeats, tempo and independent copies',()=>{const p=generate(defaults()),b=newBank(p);b.slots[1].editor=structuredClone(b.slots[0].editor);b.slots[1].editor.pattern.settings.seed='other';b.sequence=[{slot:0,repeats:2},{slot:1,repeats:1}];const ps=arrange(b,150);assert.deepEqual(ps.map(p=>p.settings.seed),[p.settings.seed,p.settings.seed,'other']);assert.ok(ps.every(p=>p.settings.bpm===150));ps[0].events[0].gain=0;assert.notEqual(b.slots[0].editor.pattern.events[0].gain,0);b.sequence[1].slot=2;assert.throws(()=>validateBank(b));});
test('sequence renders full repeated timing with continuous delay and bounded length',()=>{const p=generate({...defaults(),bars:1,bpm:240});p.events=[{...p.events[0],baseTick:0,offsetTick:0}];const fx={kick:{bypass:false,highpass:0,lowpass:20000,drive:0,delayMs:300,feedback:.7,mix:.5}};const one=renderPerformance(p,new Map(),8000,fx),two=renderSequence([p,p],new Map(),8000,fx);assert.equal(two.duration,2);assert.ok(two.channels[0].length>=16000);assert.deepEqual(two.channels[0].slice(0,7900),one.channels[0].slice(0,7900));assert.throws(()=>renderSequence(Array(171).fill(p),new Map(),8000),/170/);});
test('bank project roundtrip and legacy compatibility',()=>{const p=generate(defaults()),b=newBank(p);const project=makeProject(b.slots[0].editor,p.settings,defaultKitState(),new Map(),b);assert.deepEqual(readProject(project).project.bank,b);delete project.bank;assert.equal(readProject(project).project.bank,undefined);});
